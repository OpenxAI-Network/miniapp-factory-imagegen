[project]
name = 'miniapp-factory-imagegen'
version = '1.0.0'
description = 'Image generation server for Miniapp Factory'
dependencies = [
    "diffusers",
    "transformers",
    "torch",
    "accelerate",
    "peft",
    "xformers",
]

[project.scripts]
miniapp-factory-imagegen = "main:main"
